from django.apps import AppConfig


class SeoOptimizerConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.seo_optimizer"
    label = "seo_optimizer"
    verbose_name = "SEO Optimizer"
