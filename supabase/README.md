# ToolVerse Supabase

Migrations live in `migrations/`. Apply them on project `kxtcxncucwvbepmqkcgf`
via Dashboard SQL Editor or:

```bash
npx supabase link --project-ref kxtcxncucwvbepmqkcgf
npx supabase db push
```

See `docs/SUPABASE.md` for Auth URL and email settings.
